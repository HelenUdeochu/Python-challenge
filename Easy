OLD = 'Johnathan'

NEW = 'Adaeze'

length1 = len(OLD)
print(length1)

length2 = len(NEW)
print(length2)

if length1 > length2:
  difference = length1 - length2 

  print(difference)

print(f'The name {OLD} is longer than {NEW} by {difference} characters')
